name: ci
on:
  pull_request:
    types: [opened, synchronize]
  push:
    branches:
      - 3.x

jobs:
    automated-tests:
        uses: openeuropa/github-ci/.github/workflows/php-library-ci.yml@main
