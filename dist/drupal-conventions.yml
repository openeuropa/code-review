imports:
    - { resource: base-conventions.yml }

parameters:
    tasks.phpcs.standard: ./vendor/drupal/coder/coder_sniffer/Drupal
    tasks.phpcs.triggered_by:
        - inc
        - install
        - module
        - php
        - profile
        - theme
    tasks.phpmd.triggered_by:
        - inc
        - install
        - module
        - php
        - profile
        - theme
    tasks.phpcsfixer2.config: ./vendor/openeuropa/code-review/resources/php_cs_fixer.drupal.php
